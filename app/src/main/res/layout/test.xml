<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:binding="http://schemas.android.com/apk/res-auto">
    <data>
        <import type="luckmerlin.databinding.touch.Click"/>
        <variable name="vm" type="luckmerlin.android.TestModel" />
        <import type="luckmerlin.android.LINqiang"/>
        <import type="luckmerlin.android.TestModel"/>
    </data>
    <FrameLayout android:layout_height="match_parent" android:layout_width="match_parent">
        <Button android:text="@{vm.test??`3333`}"
            binding:m="@{Click.c().touch().id(4444).dither(true)}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </FrameLayout>
</layout>
